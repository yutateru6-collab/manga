# 漫画制作前に必読：全指示の理解・実装・検証

Version: 1.0 / 2026-09-11

## 制作開始・完成判定の必須条件

**このリポジトリを使う前に、今回の作業に関連する指示をすべて最後まで読み、全要件の意味・適用範囲・実装先・検証方法を把握すること。一部だけ読み、残りを記憶や推測で補って作り始めることを禁止する。適用される必須要件をすべて実装し、最終成果物で検証する。違反・未確認が一件でも残るものを完成版として納品してはいけない。**

「読みました」「理解しました」「プロンプトに書きました」は合格の証拠ではない。読了、理解、実装、実際の出力での検証は別の状態であり、互いの代用にしない。

対象は広告漫画、短編漫画、ネーム、画像生成用プロンプト、複数ページ作品、修正・再出力である。広告制作と物語漫画を区別し、広告用CTAや商品の訴求を物語へ無条件に持ち込まない。依頼が企画・ネーム・プロンプトのみなら、画像生成・画像検査は対象外と明記し、完成画像を確認したとは言わない。

実行環境の上位指示とユーザーの今回の明示指定を尊重する。変更が明示された条件だけを置き換え、その他の既存規則は維持する。「早く」「簡単に」を、誤字・人物違い・指定外の構図・検査省略の許可と解釈しない。作りやすくするために規則を弱めない。

## ゲートA：最新版の全文取得・読了

まず次を読む。

1. この `AGENTS.md`
2. `README.md`
3. `SKILL.md`
4. `references/image-generation-prompts.md`。漫画制作・画像生成の標準参照。短編プリセット・修正テンプレートも、使う箇所を取り違えない。
5. 今回のユーザー指示、元の原稿、指定画像・キャラクター・商品等の参照資料

次に、今回のモードで適用される規則を末尾まで読む。

- 訴求設計：`references/angle-and-hook-system.md`
- 商品・主張の根拠：`references/grounding-and-compliance.md`
- コマ割り・読み順：`references/manga-storyboard-rules.md`
- 日本語と文字組み：`references/japanese-copy-and-typesetting.md`
- 人物・場面の一貫性：`references/character-consistency.md`
- 最終画像確認：`references/visual-qa.md`
- 使用するテンプレート、および上記文書が追加で必読とする関連規則

最終広告画像の制作では該当する全段階の規則を先に把握する。物語漫画では広告専用の主張・CTA等を対象外とした理由を記録する。未読を避けるために全ファイルを形式的に対象外にしてはいけない。

ツールの省略・truncated・続きありは続きを取得する。検索断片、READMEの要約、過去の記憶、ファイル保存だけを全文読了としない。作業用にファイルパス、取得元、版またはSHA、読んだ範囲、末尾取得を記録する。同一版の全文を既に読了した相互参照は無限に再読しない。

元画像を参照する指示は、画像の説明文を読むだけで済ませず、実際の画像を開いて確認する。必要な資料が取得できない場合は未読部分を創作・推測で埋めず、利用できた資料と不足を区別する。

## ゲートB：理解を具体的な要件に変換

作業用の要件対応表に、要件ID、出典ファイル・節、適用判断と理由、解釈・合格条件、実装先・方法、最終版の検証方法、状態、証拠を記録する。長い思考過程ではなく、仕様と検査事実の短い記録にする。

原文のすべての適用される規範的な節・箇条書きを逆照合し、対応漏れを確認する。枚数、縦横比、コマ数、読み順、人物、表情、衣装、立ち位置、視線、接触関係、道具、色、文字、オチ、広告根拠、修正範囲、出力形式まで含める。人物や文字だけを確認して全指示網羅としない。

各要件は「何を守るか」「どのページ・コマ・対象に適用するか」「どこへ実装するか」「何を見れば違反を検出できるか」を具体化する。例えば人物の前後・左右、座る／立つ、誰が誰に触れるかは関係として固定し、曖昧な『自然な配置』だけで代用しない。

ユーザーが5枚と指定した場合、4ページの既定プリセットを優先して4枚にしない。ページ枚数とコマ数を混同しない。物語原稿や確定セリフがある場合は、勝手な筋変更・情報追加・結末変更をしない。衝突する条件は優先関係と適用モードで整理し、独断で規則を削除・緩和しない。

## ゲートC：実装・制作データの固定

ゲートA・Bの後で制作する。モード、構成、ページID、コマID、人物ID、参照画像、セリフ、読み順、色、保存名を同じ確定版へそろえる。画像生成プロンプトには、その画像に適用される条件を自己完結する形で渡す。『前と同じ』だけで固定条件を省かない。

- 人物・衣装・道具・場面の固定要素と、変更可能な要素を区別する。
- 原稿の日本語を確定文字列として保持し、長い文字列の再現を画像モデル任せにしない。最終広告では既存の文字後入れ方式を基本とする。
- 生成画像内の文字を採用する場合も、一文字ずつ確定セリフと照合する。長音・漢字・句読点の欠落や重複を見逃さない。
- 修正では変更対象、変更内容、保持対象を明記する。正しい顔・衣装・背景・構図まで無断で作り直さない。
- 商品の機能・価格・実績・UI・ロゴなどは参照資料に基づく。未確認のものを見栄えのために捏造しない。

プロンプトへ書いた段階は「実装済み未検証」であり、生成結果が条件を満たしたことにはならない。

## ゲートD：全ページ・全コマ・全文字の最終検証

最終画像を実際にすべて開く。一覧画像だけ、1枚目だけ、プロンプトの文章だけで完成扱いにしない。各ページを読める大きさで確認し、各コマ・セリフ・人物の指定を照合する。スマートフォン向けなら掲載想定の縮小状態での読みやすさも確認する。

最低限の分野別確認：

- 指定枚数と縦横比、ページ順、コマ数、読み順が正しい。
- 指定された人物の顔・髪・年齢感・衣装・体の素材・位置・表情・視線・接触関係が各コマで守られている。
- 再登場する人物・道具・乗り物・場面・色の扱いが、参照と前後ページで一貫している。
- 吹き出しの話者、位置、順序、尾、文字の大きさ、余白が正しく、顔や重要な動作を隠さない。
- 全セリフ・ナレーション・効果音が確定文字列と対応し、誤字、欠字、重複、意味の変化、文字切れがない。
- 適用プリセットの番号・タイトル・色数・オチ、広告なら商品根拠・主張・CTAが既存規則に従う。番号禁止のユーザー指定がある場合は番号を足さない。
- 修正されたコマだけでなく、保持するはずの周辺や前後ページに新しい崩れがない。

状態を「未実装／実装済み未検証／合格／不合格／未確認／対象外」で区別する。各証拠を最終画像の版またはハッシュ、ファイル名、ページ・コマ番号、確認結果と結びつける。証拠なし、旧版の画像、読めない文字を合格にしない。

**完成版として納品可能＝必読資料の全文読了 AND 全要件の理解・実装 AND 全適用要件の最終版検証合格 AND 違反・未確認0件。**

修正・文字差し替え・再生成・切り抜き・サイズ変更後は、古い検査結果をそのまま使わない。変更画像を再検査し、画像変更が影響する人物の一貫性・前後関係・読み順・文字を再照合する。最終出力一式が確認した画像そのものであることを確かめる。

不合格・未確認があれば原因となる最小単位を直して再検査する。解消できない場合は実際にできた範囲と不足を明示し、途中成果を完成画像に見せかけない。『指示どおり』『全ページ確認済み』は証拠がある場合に限る。

## 記録・更新作業の扱い

読了記録、要件対応表、生成・検査記録は作業用に保持し、漫画の絵や吹き出しに制作説明を混ぜない。既存SKILLの引継ぎ情報と今回のユーザーの納品指定は維持する。公開リポジトリに制作記録を無断で追加しない。

プロンプトの更新だけを依頼された場合は、対象文書と依存する規則、既存内容の保持、変更差分、参照先、保存結果を検証する。漫画の生成や全コマ画像検査は対象外であり、実施済みと報告しない。再開時は仕様・原稿・画像・検査の版と未確認事項を確認し、失われた文脈は再取得する。

この指示の追加だけで、自動画像検査プログラム、技術的な納品遮断、既存漫画の修正、将来の違反ゼロが実現したとは説明しない。既存の `scripts/validate_skill.py` によるスキル文書検査と、生成画像そのものの内容・文字・見た目の検査も区別する。
